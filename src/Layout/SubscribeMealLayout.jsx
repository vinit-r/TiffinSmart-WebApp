import React from "react";
import { Outlet } from "react-router-dom";

const SubscribeMealLayout = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default SubscribeMealLayout;
